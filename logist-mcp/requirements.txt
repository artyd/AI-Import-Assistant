# logist-mcp Python service dependencies.
# Pinned MCP SDK (FastMCP lives in mcp.server.fastmcp); the rest are the HTTP +
# HTML-parse stack the 6 tools use. Kept minimal — no secrets, all upstreams public.
mcp==1.9.4
httpx>=0.27,<1.0
beautifulsoup4>=4.12,<5.0
pydantic>=2.6,<3.0
# Starlette + Uvicorn power the plain-REST surface the TS backend calls. They are
# transitive deps of mcp==1.9.4, but pinned explicitly here since we import them.
starlette>=0.37,<1.0
uvicorn>=0.29,<1.0
# Styled .xlsx report builder for the consolidated-analysis export.
xlsxwriter>=3.1,<4.0
